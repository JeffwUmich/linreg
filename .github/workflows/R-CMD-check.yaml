# Workflow derived from https://github.com/r-lib/actions/tree/v2/examples
# Need help debugging build failures? Start at https://github.com/r-lib/actions#where-to-find-help
on:
  push:
    branches: [main, master]
  pull_request:

name: R-CMD-check.yaml

permissions: read-all

jobs:
  R-CMD-check:
    runs-on: ${{ matrix.config.os }}

    name: ${{ matrix.config.os }} (${{ matrix.config.r }})

    strategy:
      fail-fast: false
      matrix:
        config:
          - {os: macos-latest,   r: '4.3.1'}
          - {os: windows-latest, r: 'release'}
          - {os: ubuntu-latest,   r: 'devel', http-user-agent: 'release'}
          - {os: ubuntu-latest,   r: 'release'}
          - {os: ubuntu-latest,   r: 'oldrel-1'}

    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
      R_KEEP_PKG_SOURCE: yes

    steps:
      # Checkout the repository
      - uses: actions/checkout@v4

      # Update Homebrew and install qpdf on macOS
      - name: Update Homebrew and Install qpdf (macOS only)
        if: runner.os == 'macOS'
        run: |
          brew update --auto-update
          brew install qpdf

      # Set up Pandoc for RMarkdown
      - uses: r-lib/actions/setup-pandoc@v2

      # Set up R environment with setup-r@v3
      - uses: r-lib/actions/setup-r@v3
        with:
          r-version: ${{ matrix.config.r }}
          macos-arch: x86_64     # Use Intel architecture
          install-qpdf: false    # Prevent automatic qpdf installation
          use-public-rspm: true  # Use public RSPM for faster package installation

      # Install R dependencies
      - uses: r-lib/actions/setup-r-dependencies@v2
        with:
          extra-packages: any::rcmdcheck
          needs: check

      # Run R CMD check
      - uses: r-lib/actions/check-r-package@v2
        with:
          upload-snapshots: true
          build_args: 'c("--no-manual","--compact-vignettes=gs+qpdf")'
